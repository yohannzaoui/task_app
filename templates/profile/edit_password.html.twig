{% extends 'base.html.twig' %}

{% block title %}Edit password{% endblock %}

{% block body %}

    <h1>Edit my password</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary" type="submit">Send</button>
    <button class="btn btn-danger" type="reset">Erase</button>
    {{ form_end(form) }}

{% endblock %}