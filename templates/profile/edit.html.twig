{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-lg-8">
            {{ title | h1center }}
            <p><a class="btn btn-danger btn-sm" href="{{ path('edit_password') }}">{% trans %}Edit my password{% endtrans %}</a></p>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-success btn-sm"><i class="fas fa-check fa-2x"></i></button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}