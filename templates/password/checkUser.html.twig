{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-lg-6">
            {% if app.user is null %}

                {{ title | h1 }}

                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-primary btn-sm">Envoyer</button>
                <button type="reset" class="btn btn-danger btn-sm">Effacer</button>
                {{ form_end(form) }}

            {% else %}
                <h3>Vous êtes déja connecter</h3>
            {% endif %}
        </div>
    </div>
{% endblock %}