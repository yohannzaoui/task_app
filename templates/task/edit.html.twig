{% extends 'base.html.twig' %}

{% block body %}

    <h1>Edit my task</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">send</button>
    {{ form_end(form) }}

{% endblock %}