{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-lg-8">
            {{ title | h1 }}

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary" type="submit">{% trans %}Send{% endtrans %}</button>
            <button class="btn btn-danger" type="reset">{% trans %}Reset{% endtrans %}</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}